# 2026-09-11 后端上线记录

竞赛 API 与 Java 综测后端已升级到 2.0.0，服务地址沿用原配置：

- 竞赛与学生工作台：`http://38.207.179.218:18086`
- 综测与本人成绩预览：`http://38.207.179.218:18088`
- 团支书管理页面：`http://38.207.179.218:18089`

用户授权接受本次展示的 SSH ED25519 指纹后，连接固定匹配该指纹，未覆盖本机旧记录；未把用户的授权当作独立控制台核验。

## 已验证

- 公网健康、通知、能力状态和 84 项目录接口正常，管理网页返回 HTTP 200。
- 原 106 条通知全部保留。重启 API 后仍保留，SQLite 完整性检查为 ok，22 张表存在。
- 现有 MySQL 账号 pexitong3 连接 qut_zhcp 成功，8 张业务表保留。
- 平台个人记录、消息与 AI 任务的未登录请求被拦截；教务成绩接口未登录返回业务码 401。
- QQ SMTP 在服务器和 API 容器内分别通过真实认证。发信配置已启用，没有给测试流程伪造送达状态。
- DeepSeek 模型列表请求成功。原默认模型不在账号返回的可用列表中，因此改为账号实际提供的 deepseek-flash；一条仅含合成测试文字的 JSON 请求返回 HTTP 200 且结果校验成功，使用 61 tokens，没有上传学生资料。这验证连接与协议，不等同于简历质量验收。
- 文本任务显式使用非思考模式，避免短 JSON 输出被默认推理占用。参数依据 [DeepSeek 官方思考模式说明](https://api-docs.deepseek.com/guides/thinking_mode/)，模型名称以实际账号返回为准。
- 更新模型兼容配置后，本机后端 36 项回归仍通过。原 Android 47 项综合检查记录中的其余 11 项未因后端配置变更而冒称重新运行。

## 私有配置与备份

发信授权码、数据库密码和 API key 只保存在服务器 `/opt/qut-competition/.env`，权限 600，不进入公开源码或 APK。

备份位置：`/opt/qut-competition-backups/before-2.0.0-20260911-225644`。

其中包括 notices.db、qut_zhcp.sql、综测证明文件、旧项目源码和原私有配置。该目录权限 700，不能公开上传。旧镜像保留标签：

- `qut-competition-api:before-2.0.0-20260911-225644`
- `qut-zhcp-api:before-2.0.0-20260911-225644`

需要回退程序时，在 Compose 中将 api 与 zhcp-api 的 image 指向对应旧镜像，再执行：

```bash
cd /opt/qut-competition
/usr/local/bin/docker-compose -p qut-competition -f compose.yaml up -d --no-deps --no-build api zhcp-api
```

默认保留当前数据库及数据卷，不要执行 down -v。新平台数据采用新增表，单纯程序回退无需自动覆盖数据库。数据库还原会丢失备份之后的变更，应先另做当前备份并停用对应服务，再由管理员确认具体恢复范围。

其他项目容器未被操作。原综测管理页面继续沿用，未重建界面容器。

## 还需要真实用户确认

学生首次验证码到达收件箱、首次邮箱绑定和订阅效果；学校真实账号的教务成绩端到端读取；个人简历实际修改质量。没有测试邮件送达、虚构成绩、商家订单或支付成功记录。
